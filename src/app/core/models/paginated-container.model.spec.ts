import { PaginatedContainer } from './paginated-container.model';

describe('PaginatedContainer', () => {
  it('should create an instance', () => {
    expect(new PaginatedContainer()).toBeTruthy();
  });
});
